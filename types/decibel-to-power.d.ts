/**
 * Convert a dB into power gain
 *
 * @param {number} val - Value to convert
 * @return {number}
 * @example
 * import { decibelToPower } from '@ircam/sc-utils';
 * decibelToPower(0);
 * // > 1
 */
export function decibelToPower(val: number): number;
//# sourceMappingURL=decibel-to-power.d.ts.map