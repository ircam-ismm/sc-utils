/**
 * Convert a linear gain into dB
 *
 * _Alias:_ `linearToDecibel`
 *
 * @param {number} val - Value to convert
 * @return {number}
 * @example
 * import { atodb } from '@ircam/sc-utils';
 * atodb(0);
 * // > 1
 */
export function atodb(val: number): number;
//# sourceMappingURL=atodb.d.ts.map