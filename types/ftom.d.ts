/**
 * Convert a frequency in Hz to a MIDI note
 *
 * @param {number} freq - Frequency to convert
 * @return {number}
 * @example
 * import { ftom } from '@ircam/sc-utils';
 * const freq = ftom(440);
 * // > 69
 */
export function ftom(freq: number): number;
//# sourceMappingURL=ftom.d.ts.map