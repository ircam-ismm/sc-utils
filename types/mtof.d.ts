/**
 * Convert a MIDI note to frequency
 * @param {number} midiNote - MIDI Note to convert
 * @return {number}
 * @example
 * import { mtof } from '@ircam/sc-utils';
 * const freq = mtof(69);
 * // > 440
 */
export function mtof(midiNote: number): number;
//# sourceMappingURL=mtof.d.ts.map