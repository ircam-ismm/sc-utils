/**
 * Convert a linear gain into dB
 * @param {number} val - Value to convert
 * @return {number}
 * @example
 * import { decibelToPower } from '@ircam/sc-utils';
 * decibelToPower(0);
 * // > 1
 */
export function powerToDecibel(val: number): number;
//# sourceMappingURL=power-to-decibel.d.ts.map