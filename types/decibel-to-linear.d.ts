/**
 * Convert a dB into linear gain (i.e. gain)
 *
 * _Alias:_ `dbtoa`
 *
 * @param {number} val - Value to convert
 * @return {number}
 * @example
 * import { decibelToLinear } from '@ircam/sc-utils';
 * decibelToLinear(0);
 * // > 1
 */
export function decibelToLinear(val: number): number;
//# sourceMappingURL=decibel-to-linear.d.ts.map